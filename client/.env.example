# Client Environment Variables
# Next.js uses same-origin API routes by default.
NEXT_PUBLIC_API_URL=/api
# If you host the API separately, point this at the public backend URL.
# NEXT_PUBLIC_API_URL=https://your-backend-domain.com/api
