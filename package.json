{
  "name": "ai-cost-intelligence",
  "private": true,
  "description": "Run API + Vite together (fixes proxy ECONNREFUSED if only client was started)",
  "scripts": {
    "build": "npm run build --prefix client",
    "install:client": "npm install --prefix client",
    "dev": "concurrently -n api,web -c blue,magenta \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "start:server": "npm start --prefix server",
    "start:client": "npm run dev --prefix client"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
